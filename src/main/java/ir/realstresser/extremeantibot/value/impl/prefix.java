package ir.realstresser.extremeantibot.value.impl;

import ir.realstresser.extremeantibot.value.ValueHandler;
import ir.realstresser.extremeantibot.value.ValueInformation;

@ValueInformation(CategoryName = "prefix", name = "prefix", DefaultValBool = false, DefaultValStr = "[ExtreneAntiBot]", DefaultValInt = 0)
public class prefix extends ValueHandler {
    public prefix(){
        super("");
    }
}
